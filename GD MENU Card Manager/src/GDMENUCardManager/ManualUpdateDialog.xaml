<Window x:Class="GDMENUCardManager.ManualUpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Manual Update Required" SizeToContent="Height" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message -->
        <StackPanel Grid.Row="0">
            <TextBlock x:Name="ReasonText" TextWrapping="Wrap" Margin="0 0 0 10"/>

            <TextBlock TextWrapping="Wrap" Margin="0 0 0 10" Text="To upgrade, download the release package from the GitHub repository:"/>

            <TextBlock Margin="0 0 0 0">
                <Hyperlink RequestNavigate="ReleasesLink_RequestNavigate" NavigateUri="https://github.com/DerekPascarella/openMenu-Virtual-Folder-Bundle/releases" ToolTip="Open the releases page in your web browser">https://github.com/DerekPascarella/openMenu-Virtual-Folder-Bundle/releases</Hyperlink>
            </TextBlock>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 15 0 0">
            <Button Content="Skip This Version" Click="SkipButton_Click" MinWidth="120" Padding="15,8" Margin="0 0 10 0"
                    ToolTip="Ignore this version and don't ask again until the next release"/>
            <Button Content="OK" Click="OkButton_Click" MinWidth="90" Padding="15,8" IsDefault="True"
                    ToolTip="Close this notice"/>
        </StackPanel>
    </Grid>
</Window>
